#!/bin/sh
# You need this line at the top of every shell script, don't remove it

# This script sets up some environment variables for the project to work at it's fullest potential
# It's probably unnecessary for this project, but for larger projects files like this will exist, or you'll need to make your own
# Change into this directory and run with this command : 
# . env_setup.sh

# Provides the root for the project, you must be in the directory where this file is located in order for this variable to work properly
# whereami = $(pwd)
